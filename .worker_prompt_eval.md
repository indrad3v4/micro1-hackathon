WORKER MISSION — card t_5f39cf80 (EVAL — baseline + 10 кейсаў, Measured Improvement 15 балаў)

Deliverable (hard requirement):
/root/.hermes/micro1-hackathon/eval/baseline_run.md

Acceptance criteria:
AC1: eval/baseline_run.md exists with metrics per case: primary outcome + human time per task + cost per task.
AC2: >=10 кейсаў (брыфаў), з іх роўна 1 складаны edge-кейс.
AC3: параўнальная табліца baseline vs advanced (Creative Court) па кожным кейсе + кароткая методалогія (як лічылі).

Workspace: /root/.hermes/micro1-hackathon/creative-court (code) — run everything there.
Budget: $0 — DO NOT call paid APIs, no API keys, use the deterministic heuristic fallback (runs offline).

Context you need (self-contained):
- The code is a two-agent pipeline: Creator (brief -> 6 creative directions) + Judge (rubrics -> verdicts, veto).
- Run it offline per brief: `cd /root/.hermes/micro1-hackathon/creative-court && uv run creative-court demo --brief-file <brief.json> --trace-path traces/eval_<n>_<slug>.jsonl` (check cli.py --help for exact flags first; adapt if flags differ).
- demo_briefs/ is EMPTY. Create 10 brief JSON files there matching the Brief model in src/creative_court/core/models.py (fields: title, description, audience, constraints, goal — check models.py for exact field names/types). 9 normal briefs (product/ad/design/marketing variety, RU) + 1 complex edge case (e.g. a brief that is internally contradictory or violates constraints — designed to expose judge failure modes).
- Baseline definition (from official hackathon PDF): ONE simple prompt / simple script on the SAME 10 briefs — honest comparison. Implement baseline as a minimal single-prompt pass (e.g. a tiny script that asks one generic "generate one creative idea" heuristic or a trivial single-direction generator without rubrics/veto/trace). Explain the resource difference (baseline: 1 call/deterministic pass, no rubrics, no trace; advanced: 6 frames + rubric scoring + veto + JSONL trace).
- For each of the 10 cases run BOTH baseline and advanced, collect: primary outcome metric (e.g. # of directions, rubric total of best direction, whether edge case was flagged), human time per task (measure/estimate honestly, state method), cost per task ($0.00 — offline heuristic; state that LLM-mode cost would differ).
- Write /root/.hermes/micro1-hackathon/eval/baseline_run.md in the language micro1 uses (English body is fine; metrics tables are numbers). Sections: Methodology, Cases (10), Results table (per-case: baseline vs advanced, primary outcome, human time, cost), Edge case analysis, Key findings (why the judge layer improves measured outcomes), Reproducibility note (exact commands).
- Keep every trace JSONL in creative-court/traces/ (they double as submission trajectories).

Rules:
- Never write deliverables outside /root/.hermes/micro1-hackathon/.
- No paid services, no credentials.
- Verify yourself: file exists, >=10 case rows, 1 edge case, metrics present. Include exact evidence (path, bytes, wc -l) in your report.
- When done: `hermes kanban --board micro1-hackathon comment t_5f39cf80 "Worker: <what was done> — evidence: <path>, <bytes>, <checks>"` — do NOT complete the card; the manager verifies and completes.
